import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: 300,
        height: 200,
        marginLeft: 20,
        marginRight: 10,
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 20,
        backgroundColor: "#FFF"
    },
    logo: {
        width: "100%",
        height: 200,
        borderRadius: 20
    }
})